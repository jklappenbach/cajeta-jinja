a={{ (depth + 1) * 2 ** 3 ~ '!' }} b={{ items[depth]['w'] if items[depth].w > 1.5 else 'small' }}
c={{ (items | length) if false else items[1:][0].tag.upper() }} d={{ not (depth < 1 or missing is defined) }}
e={{ {'sum': nums[0] + nums[-1], 'mid': nums[1:-1]} }} f={{ 'x' * depth ~ '-' ~ (7 // 2) }}
g={{ dict(lo=nums[0], hi=nums[-1]).get('hi') }} h={{ ('one,two,three').split(',')[2 - depth] }}
